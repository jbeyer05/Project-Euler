require 'pp'

count up, finding all possible factor sets for various numbers, for each factor set, see if sum is the same as product

n = 2

$min_prod_sum_by_k = {}

while $min_prod_sum_by_k.keys.length < 100
  (2..n).each do |factor|
  end

  n += 1
end
